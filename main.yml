-- 1. Create the Gym Progress table
CREATE TABLE gym_progress_vault (
    id SERIAL PRIMARY KEY,
    member TEXT NOT NULL,
    exercises_done TEXT NOT NULL,
    cost DECIMAL(10, 2),
    statut_session TEXT DEFAULT 'completed'
);

-- 2. Insert the session data
INSERT INTO gym_progress_vault (member, exercises_done, cost)
VALUES 
('Claire', 'Yoga', 10.00),
('Kai', 'Treadmill', 15.00),
('Kealani', 'Biking', 20.00);
